<UserControl x:Class="TSBFTPPortal.Views.TabControlMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TSBFTPPortal.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <Grid>
		<TabControl x:Name="TabControlMain">
			<TabItem Header="County Specific">
			</TabItem>
			<TabItem Header="Admin System">
				<TabItem.Style>
					<Style TargetType="TabItem">
						<Style.Triggers>
							<DataTrigger Binding="{Binding SelectedCounty.CAMASystem}" Value="AS400">
								<Setter Property="Visibility" Value="Collapsed" />
							</DataTrigger>
							<DataTrigger Binding="{Binding SelectedCounty.CAMASystem}" Value="CAI">
								<Setter Property="Visibility" Value="Collapsed" />
							</DataTrigger>
							<DataTrigger Binding="{Binding SelectedCounty.CAMASystem}" Value="Custom">
								<Setter Property="Visibility" Value="Collapsed" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</TabItem.Style>
			</TabItem>
			<TabItem Header="Cama System">
				<TabItem.Style>
					<Style TargetType="TabItem">
						<Style.Triggers>
							<DataTrigger Binding="{Binding SelectedCounty.CAMASystem}" Value="AS400">
								<Setter Property="Visibility" Value="Collapsed" />
							</DataTrigger>
							<DataTrigger Binding="{Binding SelectedCounty.CAMASystem}" Value="CAI">
								<Setter Property="Visibility" Value="Collapsed" />
							</DataTrigger>
							<DataTrigger Binding="{Binding SelectedCounty.CAMASystem}" Value="Custom">
								<Setter Property="Visibility" Value="Collapsed" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</TabItem.Style>
			</TabItem>
			<TabItem Header="PAB">
				<local:PABTreeViewView />
			</TabItem>
			<TabItem Header="GIS">
			</TabItem>
			<TabItem Header="PTR">
			</TabItem>
		</TabControl>
	</Grid>
</UserControl>
