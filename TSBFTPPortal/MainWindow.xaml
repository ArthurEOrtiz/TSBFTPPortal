<Window x:Class="TSBFTPPortal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TSBFTPPortal" xmlns:views="clr-namespace:TSBFTPPortal.Views"
				mc:Ignorable="d"
        Title="TSB FTP Portal" 
				Height="450" 
				MaxHeight="800"
				Width="600"
				MaxWidth="800"
				>
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto" />
			<RowDefinition Height="auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<TextBlock 
			Text="{Binding SelectedCounty.Name}" 
			Margin="5"
			FontSize="14"
			FontWeight="Bold"
			Grid.Row="0" />
			
		<views:SearchBarView 
			Grid.Row="1"
			Grid.Column="2"
			Width="200"
			HorizontalAlignment="Right"
			Margin="0 1 2 1"
			BorderBrush="Gray"
			BorderThickness="1"
			DataContext="{Binding SearchBarViewModel}"/>
			
		<views:TabControlMainView 
			Grid.Row="2" 
			Grid.ColumnSpan="2"
			DataContext="{Binding TabControlMainViewModel}"/>
	</Grid>
</Window>
